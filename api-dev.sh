#!/usr/bin/bash
# 이 스크립트는 더 이상 사용하지 않습니다.
#
# 예전: WSL에서 직접 uvicorn 실행 + .env의 localhost 의존
# 지금: 모든 환경을 도시락(Docker) 한 벌로 통일 — 로컬도 운영 서버도 동일
#
# 켜는 법: 프로젝트 루트의 dev-start.ps1 실행
echo "이 스크립트는 폐기되었습니다."
echo "프로젝트 루트의 dev-start.ps1 을 사용하세요."
exit 1
