[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 모노레포 루트 + vendor 디렉터리 + vendor/rag (run_qa·prompt_builder·update_vectorstore) 를 pythonpath에 추가
pythonpath = .. ../vendor ../vendor/rag
